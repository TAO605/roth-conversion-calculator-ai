# V1.3 UI No-AI Refactor Addendum

Date: 2026-06-02

Source document:

- `C:\Users\86189\Documents\WXWork\1688855413696075\Cache\File\2026-06\罗斯转换计算器 V1.0 去AI味UI终极改造指令文档.docx`
- Extracted text: `docs/source-doc-extracts/roth-conversion-calculator-v1-ui-no-ai-instructions.txt`

## Assessment

The source document is directionally useful for removing low-trust template styling from the calculator UI, but it assumes a single-file `index.html` implementation. The live product is a Next.js SEO application with structured data, guide pages, analytics, Search Console evidence, release notes, tests, and Vercel/GitHub deployment automation.

Therefore this document is accepted as a visual refactor brief, not as an instruction to replace the application architecture.

## Safe Adaptation

Non-negotiables:

- Do not change Roth conversion calculation logic.
- Do not remove homepage JSON-LD, canonical SEO metadata, sitemap, robots, GA4, GSC verification, or evidence workflows.
- Do not remove the AI explainer feature; demote AI from the brand/hero hierarchy and keep it as a low-hierarchy explanation surface.
- Do not claim tax advice, optimal conversion amounts, guaranteed accuracy, or personalized recommendations.
- Keep all original inputs available, including advanced assumptions and state shortcut examples.

Accepted visual direction:

- Replace glass, blur, glow, heavy shadows, large rounded corners, and translucent panels with plain white/bordered financial-tool surfaces.
- Use a restrained primary navy (`#0A2463`), neutral borders, existing success/danger colors, and monospace numeric values.
- Make the calculator feel like a professional planning worksheet rather than an AI landing page.
- Keep mobile first-screen density and performance gains from previous rounds.

## Execution Plan

P1 homepage/core calculator surface:

- Shared `Card`, `Button`, and `Field` primitives use plain borders, 4px radius, no shadows, no glass/blur, and tighter focus rings.
- Homepage hero uses `Roth Conversion Calculator 2026` and `2026 tax estimate` instead of AI-first brand copy.
- Calculator input/result layout uses a 40/60 desktop split (`lg:grid-cols-5`, input `lg:col-span-2`, result `lg:col-span-3`).
- Quick Estimate, Advanced assumptions, presets, result cards, and advanced details use plain bordered surfaces.
- Primary result numbers use monospace 32px bold styling.

P2 supporting calculator panels:

- Review AI explainer, tax warnings, projection, payment comparison, and detailed breakdown for leftover glass or decorative treatment.
- Preserve their current placement and compliance wording.
- Demote the explainer from AI-first visible labels to a low-hierarchy educational explanation assistant.
- Use plain bordered surfaces for tax impact warnings, payment comparison, projection chart, calculation breakdown, and scope badges.
- Keep warning semantics through copy, icons, and border color rather than large tinted panels or shadows.
- Use monospace numeric emphasis in supporting result panels where projected values or formula outputs are shown.

P3 operations/content pages:

- Review long-form operational pages only after homepage calculator UI stabilizes.
- Keep content pages readable and SEO-aligned; do not chase a sitewide visual rewrite in one release.

P3 homepage detail polish:

- Keep result actions visually stable while lazy-loaded report and CPA packet modules hydrate.
- Use disabled, size-stable placeholders for lazy action buttons instead of returning `null`.
- Keep the action toolbar on a predictable two-column mobile grid and four-column desktop grid.
- Preserve the current result order: scope badges, primary estimates, secondary actions, warnings, payment comparison.
- Do not change calculation logic, report contents, CPA packet contents, or YMYL/compliance language.

P4 mobile input density:

- Keep the first Quick Estimate view focused on conversion amount, current taxable income, filing status, state marginal tax rate, and traditional IRA balance.
- Move projection-only assumptions such as retirement age and expected annual return into a collapsed `Projection assumptions` disclosure inside Quick Estimate.
- Keep projection assumptions available without moving them into the separate Advanced assumptions block.
- Preserve default values and calculation behavior; this is a disclosure and density change, not a model change.
- Keep all original inputs available and avoid hiding YMYL boundary copy or validation errors.

P5 disclosure interaction polish:

- Keep `Projection assumptions` and `Advanced assumptions` as native `<details>/<summary>` disclosures.
- Make summary rows at least 44px tall for mobile touch comfort.
- Use a visible chevron affordance that rotates on open state.
- Hide default browser disclosure markers only when the custom chevron remains visible.
- Preserve keyboard and screen-reader behavior by avoiding custom JavaScript toggles.

P6 operations/content page surfaces:

- Start the broader operations/content-page cleanup with audit and playbook routes rather than blog/article bodies.
- Replace glass, blur, large-radius, translucent, and heavy-shadow containers with plain bordered review panels.
- Keep operational pages dense, calm, and scannable; they should feel like audit worksheets, not marketing sections.
- Preserve all existing SEO metadata, JSON-LD breadcrumbs, disclaimer text, source links, and review workflow copy.
- Add a regression guard that scans the operations routes for old template surface classes.
- Treat Launch Readiness summary counters as operations status panels and guard against oversized custom radii there too.

P7 top-level content hub surfaces:

- Continue the broader content-page cleanup with index and hub pages before editing article or guide bodies.
- Replace translucent, large-radius, hover-lift, and heavy-shadow link cards with plain bordered navigation panels.
- Keep hub pages scannable as internal-link and crawl-orientation surfaces, not decorative landing pages.
- Preserve hub metadata, JSON-LD breadcrumbs, disclaimers, routes, and link targets.
- Add a regression guard that scans top-level hub routes for old template card classes.

P8 priority educational guide surfaces:

- Continue the professional surface cleanup with priority YMYL guide pages and CPA-review pages.
- Replace translucent, large-radius, and heavy-shadow review cards with plain bordered review panels.
- Preserve guide body copy, source links, JSON-LD, breadcrumbs, disclaimers, and professional-review boundaries.
- Do not edit blog article bodies or tax calculation logic as part of this UI pass.
- Add a regression guard that scans priority guide routes for old template review-card classes.

P9 dynamic detail page surfaces:

- Continue the professional surface cleanup with non-blog dynamic SEO/detail pages before editing article bodies.
- Replace translucent, large-radius, hover-lift, and heavy-shadow panels with plain bordered review surfaces.
- Keep dynamic pages scannable as educational worksheets and route-specific calculator entry points.
- Preserve all existing metadata, JSON-LD breadcrumbs, disclaimer text, body copy, and calculator/official-source CTA targets.
- Do not edit blog article bodies or tax calculation logic as part of this UI pass.
- Add a regression guard that scans non-blog dynamic detail routes for old template surface classes.

P10 shared feature component surfaces:

- Continue the professional surface cleanup with reusable feature components that appear across homepage, methodology, and result surfaces.
- Replace translucent table wrappers, FAQ cards, scenario panels, utility controls, and compact tax-data notes with plain bordered surfaces.
- Preserve native interactions, local-storage behavior, labels, links, and calculated values.
- Do not edit tax calculation logic, blog article bodies, or SEO evidence scripts as part of this UI pass.
- Add a regression guard that scans shared feature components for old template surface classes.

P11 blog shell surfaces:

- Complete the professional surface cleanup on blog index and article shell UI without editing user-owned article body content.
- Replace translucent topic groups, post cards, author/reviewer metadata blocks, calculator CTA panels, and related-guide links with plain bordered surfaces.
- Preserve Article JSON-LD, Breadcrumb JSON-LD, canonical metadata, required disclaimer text, blog discovery evidence, and all publication validators.
- Do not edit tax calculation logic, blog article bodies, or blog SEO writing workflow as part of this UI pass.
- Add a regression guard that scans blog shell routes for old template surface classes.

P12 global professional UI guard:

- Convert the completed no-glass professional UI cleanup into a recursive source-level guard for `src/app` and `src/features`.
- Block old template classes globally so future route additions do not bypass page-specific UI tests.
- Keep page-specific tests for route semantics, but let the global guard own the shared forbidden-class baseline.
- Do not edit content, tax calculation logic, blog article bodies, SEO scripts, or structured-data generation as part of this guard pass.

P13 professional UI evidence artifact:

- Add a standalone professional UI evidence command that emits JSON for the global source guard.
- Retain that JSON in the production `production-seo-evidence` artifact alongside SEO smoke, GSC evidence, structured data, blog discovery, performance, validator, and manifest files.
- Make the SEO evidence validator require the professional UI guard result so artifact downloads prove both live SEO health and source-level UI surface health.
- Do not alter page content, tax calculation logic, blog article bodies, or structured-data generation as part of this evidence pass.

P14 SEO monitoring UI evidence review:

- Surface the retained professional UI evidence file in the visible `/seo-monitoring` artifact checklist.
- Document the human pass signal for `professional-ui-evidence-result.json`: `ok: true`, scanned `src/app` and `src/features`, `violationCount: 0`, and no violation rows.
- Keep the validator checklist aligned with `professionalUiScannedFileCount` so human review and automation read the same artifact contract.
- Do not alter page content outside the SEO monitoring operations copy, tax calculation logic, blog article bodies, or structured-data generation.

P15 SEO evidence manifest checksums:

- Add `sha256` checksums for retained source evidence files in `seo-evidence-manifest.json`.
- Keep the manifest self-entry self-describing instead of hashing the final manifest file.
- Update the visible `/seo-monitoring` artifact review copy so humans verify file names, byte sizes, checksums, run identity, and retention metadata together.
- Do not alter tax calculation logic, blog article bodies, or structured-data generation.

P16 SEO evidence manifest checksum validator:

- Add `npm run seo:evidence-manifest-validate` to verify retained file byte counts and `sha256` hashes against `seo-evidence-manifest.json`.
- Run the validator in SEO Smoke after manifest generation and before uploading `production-seo-evidence`.
- Update `/seo-monitoring` so downloaded artifact review includes re-running the checksum validator.
- Do not alter tax calculation logic, blog article bodies, or structured-data generation.

P17 retained manifest checksum validation result:

- Retain `seo-evidence-manifest-validation-result.json` in the production evidence artifact.
- Mark the validation result in `seo-evidence-manifest.json` as `postManifestValidation: true` because it is generated after manifest creation.
- Add a visible `/seo-monitoring` review item for the retained manifest checksum validation result.
- Do not alter tax calculation logic, blog article bodies, or structured-data generation.

## Acceptance Checks For P1

- Homepage no longer renders the visible strings `AI-powered 2026 estimate`, `AI Roth Conversion Calculator`, or `Ask AI after results`.
- Shared primitives no longer include `backdrop-blur`, `shadow-material`, `rounded-[20px]`, `rounded-[14px]`, `active:scale`, or `focus:ring-4`.
- Core calculator layout includes `lg:grid-cols-5`, `lg:col-span-2`, and `lg:col-span-3`.
- Result summary includes `font-mono` and `text-[32px]` on primary money values.
- Existing SEO, structured data, YMYL language guards, tests, build, deployment, and production evidence continue to pass.

## Acceptance Checks For P3 Homepage Detail Polish

- Result actions render after the primary result summary.
- Dynamic result action modules use visible disabled placeholders while loading.
- Result actions use a stable `grid-cols-2` to `md:grid-cols-4` layout, not a loading-dependent flex layout.
- Existing homepage E2E, result-action layout tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P4 Mobile Input Density

- Quick Estimate includes a collapsed `Projection assumptions` disclosure.
- Retirement age and expected annual return remain available inside that disclosure.
- Advanced assumptions remain collapsed by default.
- Calculator input layout tests, homepage E2E, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P5 Disclosure Interaction Polish

- Projection and advanced summaries include `min-h-11`.
- Disclosure summaries use visible chevrons with `group-open:rotate-180`.
- Disclosure behavior remains native `details`/`summary`, not JavaScript-controlled.
- Existing input layout tests, homepage E2E, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P6 Operations Page Surfaces

- SEO monitoring, content operations, performance audit, accessibility audit, AI compliance audit, privacy data flow, production launch, launch readiness, feedback roadmap, and tax data update pages no longer use `backdrop-blur-xl`, `shadow-material`, `rounded-[22px]`, `rounded-[20px]`, `rounded-[16px]`, `rounded-[14px]`, `bg-white/75`, or `bg-white/60`.
- Operations page shells use `rounded-lg border border-neutral-200 bg-white` and inner review panels use `rounded-md border border-neutral-200 bg-white`.
- Existing content tests, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P7 Content Hub Page Surfaces

- Site index, release notes, calculator hub, state hub, glossary hub, age scenarios, examples, filing status, basis, multi-year planning, tax interactions, and tax payment method hub pages no longer use `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, `bg-white/75`, `bg-white/70`, or `bg-white/60`.
- Hub cards use `rounded-lg border border-neutral-200 bg-white` and retain clear link labels.
- Existing hub content tests, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P8 Priority Guide Page Surfaces

- Calculator assumptions, CPA review checklist, and Roth conversion priority guide pages no longer use `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, `bg-white/75`, `bg-white/70`, or `bg-white/60`.
- Guide page shells use `rounded-lg border border-neutral-200 bg-white` and nested review panels use `rounded-md border border-neutral-200 bg-white`.
- Existing guide content tests, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P9 Dynamic Detail Page Surfaces

- Keyword landing, age scenario, basis, example, filing status, glossary, state, federal bracket, multi-year planning, tax interaction, and tax payment method detail pages no longer use `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, `bg-white/75`, `bg-white/70`, or `bg-white/60`.
- Dynamic detail panels use `rounded-lg border border-neutral-200 bg-white`; nested metric, table, and link surfaces use `rounded-md` instead of custom oversized radii.
- Existing dynamic page tests, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P10 Shared Feature Component Surfaces

- Shared feature components no longer use `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, `bg-white/75`, `bg-white/70`, `bg-white/65`, `bg-white/60`, or `bg-white/55`.
- Table wrappers, FAQ items, bracket impact cards, tax data freshness notes, scenario history rows, and theme toggle surfaces use restrained bordered surfaces.
- Existing shared feature tests, homepage E2E, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P11 Blog Shell Surfaces

- Blog index and article shell pages no longer use `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, `bg-white/75`, `bg-white/70`, or `bg-white/60`.
- Topic groups, post cards, author/reviewer metadata, calculator CTA, and related-guide links use plain bordered surfaces.
- Existing blog content tests, blog discovery evidence, release-note tests, feature-registry tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P12 Global Professional UI Guard

- The global professional UI guard recursively scans `src/app` and `src/features`.
- It blocks old glass-template classes across app and feature source, including `backdrop-blur-xl`, `shadow-material`, hover-lift, oversized custom radii, and translucent white surface classes.
- Existing page-specific UI guards, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P13 Professional UI Evidence Artifact

- `professional-ui-evidence-result.json` is generated by CI and retained in `production-seo-evidence`.
- `dns-evidence-result.json` is generated by CI and retained in `production-seo-evidence` so DNS screenshots are backed by repeatable apex, canonical host, and Vercel routing checks.
- `security-headers-evidence-result.json` is generated by CI and retained in `production-seo-evidence` so production trust reviews can verify live browser security headers instead of relying only on source configuration.
- `health-evidence-result.json` is generated by CI and retained in `production-seo-evidence` so operations reviews can verify live `/api/health` status, no-store caching, tax-year metadata, pending professional-review status, public content counts, feature counts, and absence of secret-like keys.
- `crawl-discovery-evidence-result.json` is generated by CI and retained in `production-seo-evidence` so search and AI discovery reviews can verify live robots.txt, sitemap.xml, feed.xml, and llms.txt coverage instead of relying only on broad smoke status.
- The SEO evidence validator requires the professional UI evidence object to be `ok: true`, to scan `src/app` and `src/features`, and to report zero violations.
- The evidence manifest lists `professional-ui-evidence-result.json`.
- Existing global UI guard tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P14 SEO Monitoring UI Evidence Review

- `buildSeoEvidenceArtifactReview()` includes `professional-ui-evidence-result.json`.
- The visible SEO monitoring page explains how to review `professionalUiScannedFileCount`, `scannedFileCount`, `violationCount: 0`, and the empty `violations` list.
- Existing SEO monitoring tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P15 SEO Evidence Manifest Checksums

- `seo-evidence-manifest.json` includes `sha256` for every retained source evidence file.
- The manifest self-entry remains `selfDescribing: true`.
- The visible SEO monitoring page tells reviewers to check file names, byte sizes, checksums, run SHA, workflow, and retention metadata before relying on a downloaded artifact.
- Existing SEO evidence workflow tests, SEO monitoring tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P16 SEO Evidence Manifest Checksum Validator

- `scripts/validate-seo-evidence-manifest.mjs` verifies each retained source evidence file byte count and `sha256` value.
- The SEO Smoke workflow runs `node scripts/validate-seo-evidence-manifest.mjs` before artifact upload.
- The visible SEO monitoring page references `npm run seo:evidence-manifest-validate`.
- Existing SEO evidence workflow tests, SEO monitoring tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

## Acceptance Checks For P17 Retained Manifest Checksum Validation Result

- The SEO Smoke workflow uploads `seo-evidence-manifest-validation-result.json`.
- `seo-evidence-manifest.json` includes that file with `postManifestValidation: true`.
- The visible SEO monitoring page includes a separate review item for `seo-evidence-manifest-validation-result.json`.

### Round 92 addendum: evidence provenance URLs

- Add `gitHubRunUrl` and `gitHubCommitUrl` to `seo-evidence-manifest.json` when the manifest is generated inside GitHub Actions.
- Keep local generation safe by allowing those fields to be empty when GitHub environment variables are unavailable.
- Validate URL shape in `scripts/validate-seo-evidence-manifest.mjs` so retained artifacts do not drift away from the exact Actions run and commit that produced them.

### Round 93 addendum: evidence schema version

- Add `artifactSchemaVersion` to `seo-evidence-manifest.json`.
- Validate the expected schema version in `scripts/validate-seo-evidence-manifest.mjs`.
- Use the schema version as the machine-readable contract marker when future SEO evidence artifact fields are added.
- Existing SEO evidence workflow tests, SEO monitoring tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

### Round 94 addendum: evidence generated timestamp validation

- Validate `generatedAt` in `seo-evidence-manifest.json` as an ISO timestamp.
- Retain `generatedAt` and `generatedAtRetained: true` in `seo-evidence-manifest-validation-result.json`.
- Surface timestamp review in `/seo-monitoring` so artifact creation time is checked before using a downloaded proof package for Search Console or incident notes.
- Existing SEO evidence workflow tests, SEO monitoring tests, release-note tests, feature-registry tests, full tests, build, production SEO evidence, structured data, and Lighthouse evidence continue to pass.

### Round 95 addendum: evidence provenance consistency

- Validate that `gitHubRunUrl` matches `gitHubRunId` in retained SEO evidence manifests.
- Validate that `gitHubCommitUrl` matches `gitHubSha` in retained SEO evidence manifests.
- Retain `gitHubProvenanceConsistent: true` in `seo-evidence-manifest-validation-result.json`.
- Surface provenance consistency review in `/seo-monitoring` before downloaded artifacts are used for Search Console or incident notes.

### Round 96 addendum: evidence workflow event metadata

- Restrict `eventName` in `seo-evidence-manifest.json` to known generation contexts: `local`, `push`, `schedule`, and `workflow_dispatch`.
- Require retained `gitHubWorkflow` and numeric `gitHubRunAttempt` when the manifest comes from GitHub Actions.
- Retain `gitHubWorkflowRetained: true` and `runAttemptRetained: true` in `seo-evidence-manifest-validation-result.json`.
- Surface workflow-event review in `/seo-monitoring` before downloaded artifacts are used for Search Console or incident notes.

### Round 97 addendum: evidence repository provenance

- Add `gitHubRepository` to `seo-evidence-manifest.json` when the manifest is generated inside GitHub Actions.
- Validate that retained GitHub Actions artifacts come from `TAO605/roth-conversion-calculator-ai`.
- Retain `gitHubRepositoryRetained: true` in `seo-evidence-manifest-validation-result.json`.
- Surface repository provenance review in `/seo-monitoring` before downloaded artifacts are used for Search Console retry notes or incident evidence.
- Use functional calculator input state updates so rapid mobile or automated entry cannot overwrite a previously edited field with an older render snapshot.
- Keep the E2E calculator workflow guard checking the final `$90,000 converted` breakdown after rapid entry.

### Round 98 addendum: evidence GitHub server provenance

- Validate `gitHubServerUrl` in `seo-evidence-manifest.json` as `https://github.com`.
- Retain `gitHubServerUrlRetained: true` in `seo-evidence-manifest-validation-result.json`.
- Surface GitHub server host review in `/seo-monitoring` before downloaded artifacts are used for Search Console retry notes or incident evidence.
